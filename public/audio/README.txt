Put your background music file here, named exactly:

  track.mp3

If you want to use "Year of the Deer - Kepler's Hope" specifically,
you'll need to source a licensed copy yourself (e.g. purchase/download
from a platform where you have rights to use it) — it wasn't included
here since it's copyrighted and not something that can be redistributed.

Once the file is in this folder as track.mp3, the music player button
(bottom-right of the site) will play/pause it automatically. No code
changes needed unless you rename the file — if so, update the `src`
path in src/components/MusicPlayer.jsx to match.
